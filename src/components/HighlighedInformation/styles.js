const styles = (theme) => ({
  main: {
    marginTop: 10,
    textAlign: 'left',
    backgroundColor: 'rgba(253, 200, 69, 0.3)',
    border: '2px solid rgba(253, 200, 69, .5)',
    padding: theme.spacing(2),
    borderRadius: theme.shape.borderRadius,
  },
});

export default styles;
